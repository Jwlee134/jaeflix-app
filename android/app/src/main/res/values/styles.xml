<resources>
    <style name="SplashScreenTheme" parent="SplashScreen_SplashTheme">
        <item name="colorPrimaryDark">@color/status_bar_color</item>
        <item name="android:navigationBarColor">#000000</item>
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#ffffff</item>
        <item name="android:windowBackground">#141414</item>
        <item name="android:navigationBarColor">#141414</item>
        <item name="android:scrollbarThumbVertical">@color/indicator_color</item>
        <item name="android:scrollbarThumbHorizontal">@color/indicator_color</item>
    </style>

</resources>
